proc N 100 procend

proc greater_than_zero dup 0 >= procend

proc console_log dup print procend

proc main
    N
    while greater_than_zero do
        console_log 1 - 
    end
    drop
procend

main

